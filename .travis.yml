language: go
go:
  - 1.8
  - 1.9
  - tip

os:
  - linux

script:
  - go test -v -race -parallel 5 -coverprofile=coverage.txt -covermode=atomic ./

before_install:
  - pip install --user codecov
after_success:
  - codecov
