version: 2
jobs:
  build:
    docker:
      - image: golang:1.9
        environment:
          - GOPATH: /go
    working_directory: /go/src/github.com/{{ORG_NAME}}/{{REPO_NAME}}
    steps:
      - checkout
      - run: go get -v -t -d ./...
      - run: go test -v ./...
